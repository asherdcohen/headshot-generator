import React from "react";

function Navbar() {
    return (
        <nav>
            <h1>Image Upload App</h1>
        </nav>
    );
}

export default Navbar; // Ensure that the component is exported with `default`
